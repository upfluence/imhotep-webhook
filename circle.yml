machine:
  services:
    - docker

dependencies:
  pre:
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS

deployment:
  master:
    branch: master
    commands:
      - bash contrib/release.sh
